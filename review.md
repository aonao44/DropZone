# レビューメモ

## 解決済み

### ✅ ダウンロード機能の修正 (2025/10/30)

**問題**: ダウンロードボタンを押すとブラウザに画像が表示されてしまう

**解決策**:

- `ProjectDetailClient.tsx` で通常の `<a>` タグを使用していたため、CORS 制限によりダウンロードが機能していなかった
- `DownloadButton` コンポーネント（blob 方式のダウンロード実装）を使用するように変更
- 履歴画面（`submission-logs.tsx`）では既に `DownloadButton` を使用していたため問題なし

**検証結果**:

- ✅ プロジェクト詳細画面（`/project/[slug]/view`）でのダウンロード動作確認
- ✅ 提出フォーム履歴画面（`/project/[slug]/submit` > 履歴を確認）でのダウンロード動作確認
- ✅ ファイルが正しくダウンロードされ、ブラウザ遷移が発生しないことを確認

**変更ファイル**:

- `components/ProjectDetailClient.tsx`

  - `DownloadButton` コンポーネントのインポート追加
  - ダウンロードボタンを `<a>` タグから `<DownloadButton>` に変更
  - 不要な `Download` アイコンのインポート削除

- 最新の問題
- ダウンロードできるようになったが保存したファイルが勝手に開いてしまう。保存だけでいいのでどうにかなりませんか
